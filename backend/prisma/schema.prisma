// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

// To see tables in terminal: https://www.digitalocean.com/community/questions/how-to-show-all-tables-in-postgresql

// any changes to the db:
// navigate to the backend folder
// run 'npx prisma generate'

// migrate the new db (VERY IMPORTANT)
// npx prisma migrate dev --name choose_a_desc_name


generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id        Int       @id @default(autoincrement())
  username  String    @unique
  password  String    
  sets      Set[]
}

model Set {
  id        Int       @id @default(autoincrement())
  title     String
  isPublic  Boolean   @default(false)
  tags      Tag[]     @relation("SetTags")
  cards     Card[]
  userId    Int
  user      User      @relation(fields: [userId], references: [id])
}

model Card {
  id          Int       @id @default(autoincrement())
  question    String
  answer      String
  isComplete  Boolean   @default(false)
  setId       Int
  set         Set       @relation(fields: [setId], references: [id], onDelete: Cascade)
}

model Tag {
  id        Int       @id @default(autoincrement())
  name      String    @unique
  sets      Set[]     @relation("SetTags")
}
